# run unit tests
npm run unitTest

# Scripting Preferences
- Use Node.js (JavaScript) for all scripts - Python is not installed
- Avoid using Python, complex bash/awk/sed scripts - use Node.js instead